# راهنمای راه‌اندازی فرانت‌اِند و نیازمندی‌های اتصال به بک‌اِند (پروژه Bumbul)

این راهنما برای همکار بک‌اِند کار تهیه شده است تا بتواند فرانت‌اِند پروژه **Bumbul** را روی سیستم خود راه‌اندازی کند و با نیازمندی‌ها، ساختار ارتباطی (API) و اصلاحات مورد نیاز در بک‌اِند آشنا شود.

---

## بخش ۱: راهنمای راه‌اندازی فرانت‌اِند (Frontend Setup)

ساختار فرانت‌اِند پروژه به صورت یک **Monorepo** با استفاده از ابزار مدیریت پکیج **pnpm** طراحی شده است. این ساختار شامل دو اپلیکیشن اصلی در پوشه `apps/` و یک بخش کدهای مشترک در پوشه `shared/` است:
1. **Next.js app (`apps/web`)**: مخصوص صفحات عمومی، فرود (Landing Page) و سئو (SEO). پیش‌فرض روی پورت `3000`.
2. **React SPA (`apps/app`)**: پنل اصلی کاربری، دایركتوری و داشبورد تعاملی (سایپ، چت و ...). پیش‌فرض روی پورت `5173`.
3. **Shared package (`shared/`)**: تایپ‌ها و اینترفیس‌های مشترک TypeScript پروژه.

### گام‌های راه‌اندازی:

#### ۱. نصب Node.js
مطمئن شوید که [Node.js](https://nodejs.org/) (نسخه ۱۸ یا بالاتر) روی سیستم شما نصب است.

#### ۲. نصب pnpm
از آنجایی که پروژه از ساختار Workspace استفاده می‌کند، باید ابزار `pnpm` را به صورت سراسری نصب کنید. در ترمینال دستور زیر را اجرا کنید:
```bash
npm install -g pnpm
```

#### ۳. نصب وابستگی‌ها (Dependencies)
به پوشه اصلی فرانت‌اِند پروژه وارد شوید و دستور نصب را اجرا کنید:
```bash
# وارد پوشه فرانت‌اِند شوید
cd "bumbul front/bumbull-frontend"

# نصب تمام پکیج‌های مورد نیاز کل مونو‌ریپو
pnpm install
```

#### ۴. اجرای پروژه‌ها در حالت توسعه (Development Mode)
پس از اتمام نصب، برای اجرای همزمان اپلیکیشن Next.js و React SPA دستور زیر را در ریشه پوشه فرانت‌اِند اجرا کنید:
```bash
pnpm dev
```
این دستور اسکریپت `dev` را به صورت موازی در تمام پوشه‌ها اجرا می‌کند:
* اپلیکیشن معرفی (Next.js) در آدرس: `http://localhost:3000`
* اپلیکیشن اصلی (React SPA) در آدرس: `http://127.0.0.1:5173` یا `http://localhost:5173`

---

## بخش ۲: بررسی وضعیت فعلی و «شکاف معماری»

در بررسی فرانت‌اِند و بک‌اِند موجود، یک **تضاد ساختاری مهم** وجود دارد که باید قبل از شروع اتصال حل شود:

1. **فرانت‌اِند (وضعیت شبیه‌سازی شده)**:
   کدهای فرانت‌اِند در حال حاضر کاملاً با **داده‌های شبیه‌سازی شده (Mock Data)** و Stateهای لوکال در قالب Zustand (`mockStore.ts`) کار می‌کنند و هیچ درخواستی به سرور ارسال نمی‌کنند. ورود و خروج کاربر نیز از طریق کوکی‌های موقتی مثل `mock_logged_in` و `mock_user_data` شبیه‌سازی شده است.
   
2. **بک‌اِند (وضعیت Django Template)**:
   بخش بک‌اِند (`bumbul back`) بیشتر بر پایه **Django Templates** پیاده‌سازی شده و متدهای درون `views.py` خروجی HTML برمی‌گردانند (به عنوان مثال با دستور `render(request, '...', context)`). 
   * **خوشبختانه** فایل `accounts/serializers.py` که شامل ساختار سریالایزر‌های Django REST Framework (DRF) است قبلاً نوشته شده است. 
   * **اقدام لازم**: همکار بک‌اِند کار باید ویوهای مبتنی بر DRF (یا APIViewهای معمولی) ایجاد کند تا خروجی‌های JSON به جای صفحات HTML بازگردانده شوند تا فرانت‌اِند بتواند از آن‌ها استفاده کند.

---

## بخش ۳: تنظیمات حیاتی بک‌اِند برای اتصال به فرانت‌اِند

از آنجایی که احراز هویت پروژه مبتنی بر **Session + Cookie** است و فرانت‌اِند و بک‌اِند روی پورت‌های متفاوتی اجرا می‌شوند، تنظیمات زیر در فایل `settings.py` بک‌اِند برای جلوگیری از خطاهای CORS و عدم ارسال کوکی‌ها ضروری است:

### ۱. تنظیمات CORS در Django
تنظیم پیش‌فرض `CORS_ALLOW_ALL_ORIGINS = True` در حالت احراز هویت با کوکی کار نخواهد کرد؛ زیرا مرورگرها اجازه ارسال کوکی را به دامنه‌های wildcard (`*`) نمی‌دهند. لطفاً آن را به شکل زیر تغییر دهید:

```python
# settings.py

# غیرفعال کردن دسترسی همگانی در صورت فعال بودن
CORS_ALLOW_ALL_ORIGINS = False

# تعیین دقیق مبداهای مجاز فرانت‌اِند
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
    "http://localhost:5173",
    "http://127.0.0.1:5173",
]

# مجاز کردن ارسال اطلاعات احراز هویت (کوکی‌ها و سشن)
CORS_ALLOW_CREDENTIALS = True
```

### ۲. تنظیمات Session و Cookie برای Localhost
برای کارکرد صحیح سشن‌ها روی مرورگر در محیط لوکال تنظیمات زیر را چک کنید:

```python
SESSION_COOKIE_DOMAIN = None          # روی لوکال باید None باشد
SESSION_COOKIE_SECURE = False         # برای HTTP معمولی غیرفعال باشد (در پروداکشن با HTTPS فعال شود)
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SAMESITE = 'Lax'       # یا 'None' در صورتی که دامنه‌ها کاملا متفاوت باشند (که در HTTPS الزامی است)
```

---

## بخش ۴: نیازمندی‌های API و سناریوهای تعاملی فرانت‌اِند

فرانت‌اِند نیاز دارد که در بخش‌های زیر، وب‌سرویس‌هایی با خروجی JSON داشته باشد. ساختار خروجی‌ها باید با اینترفیس‌های تعریف شده در [shared/index.ts](file:///c:/Amir/work/projects/bumbul%20project/bumbul%20front/bumbull-frontend/shared/index.ts) مطابقت داشته باشد.

### ۱. فرآیند ثبت‌نام و تایید شماره (Telegram OTP)
ثبت‌نام پروژه به صورت چند مرحله‌ای (Multi-step) است:
* **مرحله ۱ (مشخصات اولیه)**: 
  * درخواست: `POST /accounts/register/`
  * ورودی: `{ first_name, last_name, date_of_birth, gender, password, invite_code }`
  * خروجی: ذخیره موقت اطلاعات در سشن/کش و بازگرداندن کد تایید شماره (OTP) تولید شده برای نمایش به کاربر.
* **مرحله ۲ (تایید تلگرام)**:
  * فرانت‌اِند کاربر را به ربات تلگرام (`t.me/bumbullbot`) هدایت می‌کند. کاربر کد را به بات می‌فرستد.
  * فرانت‌اِند هر ۳ ثانیه یک‌بار به آدرس `GET /accounts/api/profile-completion/` پولینگ می‌زند تا بفهمد شماره تایید شده است یا خیر.
  * همچنین بات تلگرام پس از تایید شماره، یک لینک بازگشت (Magic Link) حاوی توکن به آدرس فرانت‌اِند ارسال می‌کند (مثال: `http://localhost:5173/register?token=...`). بک‌اِند باید وب‌سرویسی برای اعتبارسنجی این توکن و ورود کاربر داشته باشد.
* **مرحله ۳ (تکمیل پروفایل)**:
  * درخواست: `POST /accounts/register/` (یا ادیت پروفایل) برای ثبت علایق (Interests)، قد، شهرهایی که زندگی می‌کند و بیوگرافی.
* **مرحله ۴ (آپلود تصاویر)**:
  * درخواست: `POST /accounts/photos/upload-multiple/` به صورت `multipart/form-data` برای ارسال ۳ الی ۷ تصویر.

### ۲. ورود و خروج (Login / Logout)
* **ورود با رمز عبور**: `POST /accounts/login/` با بدنه `{ phone_number, password }`. سشن لاگین ایجاد کرده و کوکی `sessionid` را تنظیم کند.
* **ورود با تلگرام**: ثبت شماره موبایل، ارسال کد یکبار مصرف به بات تلگرام و ورود خودکار پس از تایید توسط بات.
* **خروج**: `POST /accounts/logout/` برای حذف سشن.

### ۳. داشبورد و لایک/دیس‌لایک (Swipe Screen)
* **دریافت کارت بعدی**: `GET /accounts/swipe/` برای دریافت مشخصات کاربر بعدی جهت نمایش کارت (نام، بیو، تصاویر، سن، علایق).
* **ثبت حرکت سایپ**: `POST /accounts/swipe/action/` با بدنه `{ swiped_user_id, swipe_type }` که مقدار `swipe_type` یکی از موارد `like`، `pass` یا `super` است. 
  * خروجی مورد انتظار: `{ success: true, match_created: boolean, match_id: int|null }`
  * در صورت ایجاد Match، فرانت‌اِند صفحه افکت Match Alert را باز می‌کند.

### ۴. چت آنلاین و وب‌سوکت (Real-time Chat)
* **اتصال وب‌سوکت**: فرانت‌اِند تلاش می‌کند به آدرس زیر متصل شود:
  `ws://<server-domain>/ws/chat/<match_id>/` (یا `wss://` در محیط پروداکشن).
  بک‌اِند باید با استفاده از Django Channels وب‌سوکت چت را پیاده‌سازی کند.
* **قالب ارسال پیام**: `{ "type": "message", "content": "text" }`
* **قالب دریافت پیام**: `{ "type": "message", "sender_id": int, "content": "text", "timestamp": "ISO Date" }`

### ۵. لیست APIهای مورد نیاز (Endpoints Summary)

| مسیر API | متد | شرح عملکرد | ورودی / خروجی |
|---|---|---|---|
| `/accounts/login/` | `POST` | ورود کاربر | بدنه: نام کاربری و پسورد / خروجی: جزئیات کاربر + کوکی سشن |
| `/accounts/logout/` | `POST` | خروجی کاربر | بدون بدنه / کوکی سشن را باطل کند |
| `/accounts/register/` | `POST` | ثبت نام مرحله به مرحله | بدنه متناسب با مرحله / خروجی: جزئیات پیشرفت یا توکن OTP |
| `/accounts/profile/me/` | `GET` | دریافت پروفایل کاربر فعلی | بدون ورودی / سریالایزر پروفایل شخصی |
| `/accounts/profile/<slug>/` | `GET` | دریافت پروفایل عمومی کاربر | پارامتر slug / سریالایزر پروفایل عمومی |
| `/accounts/profile/edit/` | `PUT/PATCH` | ویرایش مشخصات پروفایل | بدنه فیلدهای تغییر یافته / خروجی: اطلاعات آپدیت شده |
| `/accounts/interests/` | `GET` | لیست تمام علاقه‌مندی‌ها | خروجی: آرایه‌ای از علاقه‌مندی‌ها (id, name, slug) |
| `/accounts/photos/upload-multiple/` | `POST` | آپلود گروهی عکس‌ها | فرم دیتا تصاویر / خروجی: آرایه عکس‌های ثبت شده |
| `/accounts/photos/reorder/` | `POST` | تغییر چیدمان تصاویر | بدنه: `{"orders": [{"id": 1, "order": 0}, ...]}` |
| `/accounts/photos/delete/<id>/` | `POST` | حذف یک تصویر کاربری | آیدی عکس در URL |
| `/accounts/photos/set-primary/<id>/` | `POST` | انتخاب تصویر اصلی پروفایل | آیدی عکس در URL |
| `/accounts/swipe/` | `GET` | دریافت پروفایل پیشنهادی سایپ | بدون ورودی / خروجی: یک پروفایل کاربری |
| `/accounts/swipe/action/` | `POST` | ثبت لایک/دیس‌لایک | بدنه: `{swiped_user_id, swipe_type}` / خروجی: وضعیت مچ شدن |
| `/accounts/users/` | `GET` | جستجو و دایرکتوری کاربران | پارامترهای کوئری فیلتر / خروجی: لیست کاربران پیجینیت شده |
| `/accounts/users/nearby/` | `GET` | کاربران نزدیک به موقعیت کاربر | بدون ورودی (موقعیت از سشن کاربر) / خروجی: لیست مرتب شده بر اساس مسافت |
| `/accounts/api/profile-completion/` | `GET` | درصد تکمیل بودن پروفایل | بدون ورودی / خروجی: درصد تکمیل و فیلدهای باقی‌مانده |
